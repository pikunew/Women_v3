// config.js
const config = {
   // apiUrl: process.env.NODE_ENV === 'production' ? 'https://womenhelpline.in/api' : '/api',
   apiUrl: "https://womenhelpline-backend.onrender.com",
};

export default config;
